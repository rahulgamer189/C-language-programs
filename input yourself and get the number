/*****Looping statement simple code repeating*****/
#include <stdio.h>
int main() 
{ 
    int yaaaa,x;
    printf("Enter the value of x: ");
    scanf("%d", &x);
    for(yaaaa=1; yaaaa<=x; yaaaa++) //here the code will write 1 to x// 
    {
        printf("%d ", yaaaa);
    }
    return 0;
}
