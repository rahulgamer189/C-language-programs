/*****Looping statement simple code repeating*****/
#include <stdio.h>
int main() 
{ 
    int yaaaa,x;
    printf("Enter the value of x: ");
    scanf("%d", &x);
    for(yaaaa=x; yaaaa>=1; yaaaa--) //here the code will write x to 1// 
    {
        printf("%d ", yaaaa);
    }
    return 0;
}
